module debounce(
    input wire clk,
    input wire btn,
    output reg btn_db
);

reg [15:0] count;

always @(posedge clk)
begin
    if(btn)
        count <= count + 1;
    else
        count <= 0;

    if(count == 16'hFFFF)
        btn_db <= 1;
    else
        btn_db <= 0;
end

endmodule